<!DOCTYPE html>
<html>
<head>
    <title>System Diff - {{ inventory_hostname }}</title>
    <style>
        body { font-family: sans-serif; padding: 20px; }
        table { border-collapse: collapse; width: 100%; }
        th, td { padding: 8px; border: 1px solid #ccc; }
        th { background-color: #f5f5f5; }
        .changed { background-color: #ffdddd; }
    </style>
</head>
<body>
    <h2>System Diff for {{ inventory_hostname }}</h2>
    <table>
        <tr>
            <th>Key</th>
            <th>Pre-check</th>
            <th>Post-check</th>
        </tr>
        {% for key in post_data.keys() %}
        <tr class="{% if pre_data[key] != post_data[key] %}changed{% endif %}">
            <td>{{ key }}</td>
            <td>{{ pre_data[key] }}</td>
            <td>{{ post_data[key] }}</td>
        </tr>
        {% endfor %}
    </table>
    <p><small>Generated on {{ ansible_date_time.date }} {{ ansible_date_time.time }}</small></p>
</body>
</html>
